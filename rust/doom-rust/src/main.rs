use sdl2;
mod typedefs;
mod p_player;
mod g_game_state;

fn game_loop() {
    println!("Hello, Doom!");
}


fn main() {
    // init engine
    // keyboard IO
    // events handler
    // renderer
    // player
    // rendering context (window)
    game_loop();
}

